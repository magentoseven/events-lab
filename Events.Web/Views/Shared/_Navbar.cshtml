<nav class="navbar navbar-expand-lg navbar-dark my-bg-primary navbar-fixed-top" style="position: fixed; top: 0; left: 0; width: 100%">
    @Html.ActionLink("@EVENTZ", "", "", new { area = "" }, new { @class = "navbar-brand" })
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav mr-auto">
            @if (Request.IsAuthenticated)
            {
                <li class="@(ViewContext.RouteData.Values["Action"].ToString() == "My" ? "nav-item active" : "nav-item")">
                    @Html.ActionLink("My Events", "My", "Event", null, new { @class = "nav-link" })
                </li>
                <li class="@(ViewContext.RouteData.Values["Action"].ToString() == "Create" ? "nav-item active" : "nav-item")">
                    @Html.ActionLink("Create Event", "Create", "Event", null, new { @class = "nav-link" })
                </li>
            }
        </ul>
        @Html.Partial("_LoginPartial")
    </div>
</nav>